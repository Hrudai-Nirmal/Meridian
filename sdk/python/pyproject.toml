[project]
name = "meridian"
version = "0.1.0"
description = "Python tracing SDK for Meridian AI automation telemetry."
requires-python = ">=3.9"
authors = [{ name = "Meridian" }]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
